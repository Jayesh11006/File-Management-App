<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Management App</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">
</head>

<body>
    <h1>File Management App</h1>

    <!-- Form for renaming files -->
    <form id="rename-form">
        <h2>Rename Files</h2>
        <label for="rename-directory">Directory:</label>
        <input type="text" id="rename-directory" name="directory" required>

        <label for="prefix">Prefix:</label>
        <input type="text" id="prefix" name="prefix" required>

        <label for="sequence_type">Sequence Type:</label>
        <select id="sequence_type" name="sequence_type" required>
            <option value="ascending">Ascending</option>
            <option value="descending">Descending</option>
        </select>

        <label for="new_extension">New Extension (optional):</label>
        <input type="text" id="new_extension" name="new_extension">

        <input type="submit" value="Rename Files">
    </form>

    <!-- Form for undoing renames -->
    <form id="undo-form">
        <h2>Undo Rename</h2>
        <button type="submit">Undo Last Rename</button>
    </form>

    <!-- Section to display results -->
    <div id="results" class="results" style="display: none;"></div>

    <!-- <script src="{{ url_for('static', filename='js/scripts.js') }}"></script>. -->

    <script src="/static/js/scripts.js"> </script>
</body>

</html>
